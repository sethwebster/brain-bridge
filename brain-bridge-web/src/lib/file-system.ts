// import { FileWithDirectoryAndFileHandle, directoryOpen } from "browser-fs-access";

// class BrowserFileSystemReader {

//   async openDirectoryAndReadFiles(allowedFileExtensions: string[]): Promise<FileWithDirectoryAndFileHandle[]> {
//     const files = await this.openDirectory(allowedFileExtensions);
//     return files;
//   }

//   private async openDirectory(allowedFileExtensions: string[]): Promise<FileWithDirectoryAndFileHandle[]> {
//     const blobsInDir = await directoryOpen({ recursive: true });
    
//   }

// }

// export default BrowserFileSystemReader;